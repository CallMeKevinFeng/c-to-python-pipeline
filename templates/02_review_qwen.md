你是一个资深 C/Python 安全审查专家。请对以下 C 扩展代码进行严格安全检查，并直接输出修复后的最终版本。

模块名：`{{MODULE_NAME}}`
原始代码：
```c
{{EXTENSION_CODE}}
```

要求：
1. 检查引用计数、内存泄漏、缓冲区溢出、GIL 安全、错误处理等。
2. **直接给出修复后的完整 C 扩展代码**，用文件标记格式输出，文件名必须使用给定的模块名加 `_final` 后缀。
3. 输出格式必须严格如下（不要用其他文件名）：
===FILE:{{MODULE_NAME}}_final.c===
```c
// 修复后的完整代码
```
4. 如果有严重问题，在代码注释中说明修改原因。
5. 如果代码完全没问题，输出原始的完整代码（可略作格式调整），但仍使用上述文件名格式。